package main

import (
	
)

/*
func calculate_standard_deviation(float mean, int size, float [30]prices) float64 {
	sum_square_diffs := 0
	for _, price := range prices {
		sum_square_diffs += ((prices[i] - mean) ** 2)
	}
	return math.Sqrt(sum_square_diffs/size-1)
}

func calculate_mean(arr [30]prices, int size) float64 {
	sum := 0
	for _, price := range prices {
		sum += price
	}
	return sum/size
}
*/